# Forensics Database Project


### Command To Run:  
cd Python
python -m streamlit run app.py

## Project Outline
- UI interface allowing for CRUD operations
- Run predefined queries and see output
- CMD to run SQL commands if user so requires it